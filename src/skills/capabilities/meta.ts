import type { SkillMeta } from "../types";
export const meta: SkillMeta = {
  id: "capabilities", label: "Capability check",
  description: "Check which Dex tools exist, are enabled, or need setup before deciding how to fulfill a request.",
  sensitive: false,
};
