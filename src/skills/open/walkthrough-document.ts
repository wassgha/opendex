export interface WalkthroughView {
  current: string;
  title: string;
  summary: string;
  entries: { id: string; title: string; summary: string }[];
  file?: string;
  line?: number;
  sourceExcerpt?: string;
  error?: string;
}

const escape = (s: string) => s.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]!);
export const WALKTHROUGH_ORIGIN = 'https://dex-walkthrough.invalid';
const actionLink = (action: string, target = '') => `${WALKTHROUGH_ORIGIN}/?action=${action}&target=${encodeURIComponent(target)}`;

/** No scripts, remote assets, preload bridge or source HTML execution. */
export function walkthroughDocument(view: WalkthroughView): string {
  const node = (x: number, y: number, width: number, label: string, sub: string, entries: string[]) => `<g class="${entries.includes(view.current) ? 'selected' : ''}"><rect x="${x}" y="${y}" width="${width}" height="76" rx="10"/><text x="${x + 18}" y="${y + 31}" class="node-title">${label}</text><text x="${x + 18}" y="${y + 54}" class="node-sub">${sub}</text></g>`;
  const index = view.entries.findIndex(e => e.id === view.current);
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><title>Dex · Code walkthrough</title><style>
  :root{color-scheme:dark;--bg:oklch(17% .006 250);--surface:oklch(21% .006 250);--text:oklch(93% .006 250);--muted:oklch(72% .01 250);--line:oklch(34% .01 250);--accent:oklch(82% .10 175)}
  *{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:15px/1.55 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}header{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:22px 32px;border-bottom:1px solid var(--line)}.brand{font-weight:650}.actions{display:flex;gap:10px;align-items:center;flex-shrink:0}.eyebrow{font-size:11px;letter-spacing:.12em;text-transform:uppercase;color:var(--muted)}a{color:inherit;text-decoration:none}a:focus-visible,summary:focus-visible{outline:2px solid var(--accent);outline-offset:4px}a:hover{color:var(--accent)}.button{display:inline-block;padding:9px 16px;border:1px solid var(--line);border-radius:7px;font-size:13px}.primary{background:var(--accent);border-color:var(--accent);color:var(--bg);font-weight:650}.primary:hover{color:var(--bg);filter:brightness(1.08)}.layout{display:grid;grid-template-columns:220px minmax(0,1fr);min-height:calc(100vh - 83px)}nav{padding:28px 18px;border-right:1px solid var(--line)}nav a{display:block;padding:10px 12px;margin:3px 0;border-radius:6px;font-size:13px}nav a[aria-current=step]{background:var(--surface);color:var(--accent);font-weight:600}.number{display:inline-block;width:25px;color:var(--muted);font-size:11px}nav p{padding:0 12px}main{padding:30px 36px 48px;max-width:1160px;width:100%}h1{font-size:29px;line-height:1.2;margin:9px 0 14px;letter-spacing:-.025em}h2{font-size:18px;margin:28px 0 12px}p{max-width:72ch;margin:0 0 16px}.intro{color:var(--muted);max-width:70ch}.diagram{margin:24px 0 16px;border:1px solid var(--line);border-radius:12px;padding:16px 12px;background:var(--surface);overflow:auto}svg{display:block;width:100%;min-width:560px;height:auto}rect{fill:var(--bg);stroke:var(--line);stroke-width:1.5}.selected rect{stroke:var(--accent);stroke-width:2.5}.node-title{fill:var(--text);font-size:15px;font-weight:600}.node-sub,.edge{fill:var(--muted);font-size:11px}.edge{text-anchor:middle}.wire{fill:none;stroke:var(--muted);stroke-width:1.5;marker-end:url(#arrow)}.return{stroke-dasharray:5 5}.legend{font-size:12px;color:var(--muted);margin:10px 4px 0}details{border-bottom:1px solid var(--line);padding:13px 0}summary{cursor:pointer;font-weight:550}details p{margin:12px 0 12px 18px;color:var(--muted);font-size:14px}.code-link{margin-left:18px;font-size:13px;color:var(--accent)}footer{display:flex;justify-content:space-between;align-items:center;gap:20px;margin-top:28px;padding-top:20px;border-top:1px solid var(--line)}footer p{margin:0;font-size:13px;color:var(--muted)}pre{font:12px/1.7 ui-monospace,SFMono-Regular,monospace;white-space:pre;overflow:auto;padding:20px;background:var(--surface);border:1px solid var(--line);border-radius:8px;max-height:440px}.file{font:12px ui-monospace,monospace;color:var(--accent);overflow-wrap:anywhere}.error{border:1px solid oklch(70% .12 60);padding:14px;border-radius:6px;margin-bottom:20px;color:oklch(87% .07 65)}@media(max-width:800px){.layout{grid-template-columns:170px minmax(0,1fr)}main{padding:24px 20px}header{padding:18px 20px}}@media(max-width:860px){.layout{display:block}nav{border-right:0;border-bottom:1px solid var(--line);padding:12px;display:flex;overflow:auto;gap:4px}nav p{display:none}nav a{white-space:nowrap}h1{font-size:25px}footer{align-items:flex-start}}
  </style></head><body><header><div><span class="brand">Dex</span> <span style="color:var(--muted)">/ Code walkthrough</span></div><div class="actions">${index < view.entries.length - 1 ? `<a class="button primary" href="${actionLink('next')}">${view.current === 'overview' ? 'Explore code →' : 'Next stop →'}</a>` : ''}<a class="button" href="${actionLink('end')}">End walkthrough</a></div></header><div class="layout"><nav aria-label="Tour stops"><p class="eyebrow">Explore the source</p><a href="${actionLink('start')}" ${view.current === 'overview' ? 'aria-current="step"' : ''}><span class="number">◎</span>Architecture</a>${view.entries.map((e, i) => `<a href="${actionLink('jump', e.id)}" ${e.id === view.current ? 'aria-current="step"' : ''}><span class="number">${String(i + 1).padStart(2, '0')}</span>${escape(e.title)}</a>`).join('')}</nav><main>
  ${view.error ? `<p role="alert" class="error">${escape(view.error)}</p>` : ''}<div class="eyebrow">${view.current === 'overview' ? 'Start here · The big picture' : `Source tour · ${index + 1} of ${view.entries.length}`}</div><h1>${escape(view.title)}</h1><p class="intro">${escape(view.summary)}</p>
  <figure class="diagram"><svg viewBox="0 0 790 330" role="img" aria-labelledby="map-title map-desc"><title id="map-title">How OpenDex connects your request to an action</title><desc id="map-desc">Voice and text enter the renderer, cross the preload bridge, and reach the main agent. The agent invokes skills through permissions. Replies return to the renderer. Realtime voice connects the renderer through IPC to the main session.</desc><defs><marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse"><path d="M 0 0 L 10 5 L 0 10 z" fill="currentColor"/></marker></defs>
  <text x="30" y="24" class="node-sub">INTERFACE</text><text x="310" y="24" class="node-sub">BOUNDARY</text><text x="555" y="24" class="node-sub">MAIN PROCESS · SECRETS STAY HERE</text>
  ${node(20, 58, 220, 'Voice & conversation', 'Listen · display · play replies', ['voice'])}${node(305, 58, 180, 'Preload bridge', 'Typed IPC messages', ['bridge'])}${node(550, 58, 220, 'Agent & commands', 'Model reply + tool loop', ['command', 'agent'])}
  <path class="wire" d="M240 96 H305"/><path class="wire" d="M485 96 H550"/><text class="edge" x="272" y="83">request</text><text class="edge" x="518" y="83">request</text>
  ${node(550, 222, 220, 'Skills & permissions', 'Approved actions → results', ['tools', 'walkthrough'])}${node(20, 222, 220, 'Realtime voice', 'Main hosts speech-to-speech', ['realtime'])}
  <path class="wire" d="M660 134 V222"/><text class="edge" x="710" y="184">tool calls</text><path class="wire return" d="M580 222 V173 H130 V134"/><text class="edge" x="340" y="165">reply · progress · playback</text><path class="wire return" d="M100 222 V200 H80 V134"/><text class="node-sub" x="26" y="318">Audio crosses IPC to the main realtime session.</text><text class="edge" x="391" y="259">Configuration</text><text class="edge" x="391" y="279">models · voice · enabled skills</text></svg><figcaption class="legend">Solid arrows: requests and actions. Dashed arrows: replies and audio. Highlight: your current source stop.</figcaption></figure>
  ${view.sourceExcerpt ? `<p class="file">${escape(view.file ?? '')}:${view.line ?? 1}</p><pre tabindex="0" aria-label="Current source excerpt"><code>${escape(view.sourceExcerpt)}</code></pre>` : `<h2>Explore a component</h2><p class="intro">Expand an explanation, ask Dex a question, or choose a source stop when you’re ready.</p>${view.entries.filter(e => ['voice', 'bridge', 'agent', 'tools', 'realtime', 'config'].includes(e.id)).map(e => `<details><summary>${escape(e.title)}</summary><p>${escape(e.summary)}</p><a class="code-link" href="${actionLink('jump', e.id)}">Open this source →</a></details>`).join('')}`}
  <footer><p>${view.current === 'overview' ? 'Take your time here. Code opens only when you choose a stop or say “next”.' : 'Ask Dex about this code, or choose another stop.'}</p>${index === view.entries.length - 1 ? `<a class="button" href="${actionLink('start')}">Back to architecture</a>` : `<a class="button primary" href="${actionLink('next')}">${view.current === 'overview' ? 'Begin with commands →' : 'Next stop →'}</a>`}</footer></main></div></body></html>`;
}
