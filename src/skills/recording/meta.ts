import type { SkillMeta } from "../types";
export const meta: SkillMeta = {
  id: "recording", label: "Interaction recording",
  description: "Start and stop a local screen-and-audio recording when you ask. Uses your recording settings.",
  sensitive: false,
};
