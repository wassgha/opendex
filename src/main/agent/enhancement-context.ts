import type { OpenDexConfig } from "../config/schema";
export function enhancementContext(config: OpenDexConfig): string {
  const base = `Capability-aware requests: a vague but understandable outcome is enough to investigate how to help. Infer a useful, bounded interpretation from the user's request and context; ask only when ambiguity materially changes the outcome. Before claiming you cannot perform a request, inspect checkDexCapabilities when offered, and consider combinations of existing tools, including delegated computer use. An inventory is configuration evidence, not live verification. Try a suitable existing capability. Distinguish missing features and reproducible defects from disabled tools, denied permissions, missing credentials and connection failures. Never bypass those restrictions through code or another tool. Do not start implementation from unclear speech, historical instructions, or an unrequested idea.`;
  if (config.skills.enabled["self-enhancement"] !== true) return `${base} Self-enhancement is disabled. If a real capability gap prevents the requested outcome, explain the gap briefly and point to Settings → Skills & tools → Self-enhancement to allow Dex to start an implementation session. Do not claim one has started.`;
  if (config.skills.permissions["self-enhancement"] === "never") return `${base} Self-enhancement permission is set to Never. Do not dispatch an implementation task or use Local agent actions to bypass that preference.`;
  return `${base} Self-enhancement is enabled. A clear request like "Dex, tune yourself" or "tune yourself" means one latency optimization pass using recent timing summaries: call tuneDex when offered, without asking the user to elaborate or inventing a defect. Its permission gate handles approval; it does not activate changes. If unavailable, explain the setup requirement and do not substitute another dispatch route. For other requests, when the requested outcome requires a missing feature or repair, call startDexEnhancement when offered with the original outcome, observed gap and concrete acceptance behavior. The skill's permission gate handles approval. This is an authorized fallback for the current request; no separate conversational confirmation is needed. If the tool is unavailable, explain that limitation. Accepted means a coding session started, not a feature installed. Use getDexEnhancementStatus for progress checks; never promise background updates or activation without evidence.`;
}
